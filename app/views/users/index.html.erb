<h1>All users</h1>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.username, user %>
    </li>
  <% end %>
</ul>

<%= will_paginate list_classes: %w(pagination my-3 justify-content-center) %>
