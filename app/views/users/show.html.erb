<h1><%= @user.username %></h1>
<% if @user.posts.any? %>
  <h3>Posts (<%= @user.posts.count %>)</h3>
  <ul>
    <% @posts.each do |post| %>
      <li>
        <%= link_to post.content, post %>
      </li>
    <% end %>
  </ul>
  <%= will_paginate @posts %>
<% end %>
